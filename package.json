{
  "name": "nodereact-template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "cd src/backend && npm install && npm start",
    "dev": "concurrently \"cd src/backend && nodemon\" \"cd src/frontend && npm start\""
  },
  "dependencies": {
    "concurrently": "^8.0.1",
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=12.16.1",
    "npm": ">=6.13.4"
  },
  "devDependencies": {}
}
